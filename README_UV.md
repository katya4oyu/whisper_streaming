# whisper_streaming with uv

## Installation

To install whisper_streaming with MLX support using uv:

```bash
uv add "whisper-streaming[mlx] @ git+https://github.com/katya4oyu/whisper_streaming.git@uv"
```

This will install the package directly from the uv branch of the repository with MLX extra dependencies.